bundle:
  name: shopflow-dbt

variables:
  warehouse_id:
    description: SQL Warehouse ID for the dbt task (overridden per target)
    default: "xx"
  warehouse_id_dev:
    description: SQL Warehouse ID for dev target
    default: "xx"
  warehouse_id_prod:
    description: SQL Warehouse ID for or prod target
    default: "xx"
  catalog:
    description: Unity catalog name for dbt
    default: workspace

include:
  - resources/dbt_shopflow_job.yml

targets:
  dev:
    mode: development
    default: true
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: ${var.warehouse_id_dev}
      catalog: workspace

  prod:
    mode: production
    default: false
    workspace:
      root_path: /Workspace/Users/${workspace.current_user.userName}/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: ${var.warehouse_id_prod}
      catalog: workspace
